services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    networks:
      - default

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    networks:
      - default

networks:
  default:
    driver: bridge